kind: ServiceAccount
apiVersion: v1
metadata:
  name: secrets-reader
  namespace: todoapp

---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-admin
  namespace: todoapp
rules:
- apiGroups: [""]
  resources: ["pods"]
  verbs: ["list"]

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: pod-admin-binding
  namespace: todoapp
subjects:
- kind: ServiceAccount
  name: secrets-reader
roleRef:
  kind: Role
  name: pod-admin
  apiGroup: rbac.authorization.k8s.io